
//                        ↑    ↓    ←    →    ↖    ↙    ↘    ↗
const keyInput      = [   1,   2,   4,   8,   5,   6,  10,   9];
const moveDirectX   = [   0,   0, -16,  16, -16, -16,  16,  16];
const moveDirectY   = [ -16,  16,   0,   0, -16,  16,  16, -16];
const DirectionX    = [   0,   0,  -1,   1,  -1,  -1,   1,   1];
const DirectionY    = [  -1,   1,   0,   0,  -1,   1,   1,  -1];


const playerUnit        = PVariable(list(0,7,9,10,0,0,0,0));
const userSetLight      = PVariable();
const userLight         = PVariable();
const settingMusic      = PVariable(); //재생할 곡 선택
const playingMusic      = PVariable(); //현재 재생중인 곡
const setMusic          = PVariable(); //음악재생여부
const setScreen         = PVariable(); //화면고정여부
const battleState       = PVariable(); //배틀상태
const moveState         = PVariable(list(1,1,1,1,0,0,0,0)); //0 못움직임, 1 움직임
var moveStateMSQC       = 0; //0 못움직임, 1 움직임
const mainStory         = PVariable();
const currentLocation   = PVariable(); //현재위치
const lastVisitVillage  = PVariable(); //마지막으로 방문했던 마을,도시 위치
const lastVisitHeal     = PVariable(); //마지막으로 방문했던 치료소
const checkPlaytime     = PVariable(); //1분마다 1씩 오르게
const currentTileset    = PVariable();

const using_Surfing     = PVariable(); //파도타기 상태


const selectMenu        = PVariable(); //선택중인 메뉴의 index (GUI)
const selectIcon1       = [Db("\x08▶\x1E "), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04")];
const selectIcon2       = [Db("\x04"), Db("\x08▶\x1E "), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04")];
const selectIcon3       = [Db("\x04"), Db("\x04"), Db("\x08▶\x1E "), Db("\x04"), Db("\x04"), Db("\x04")];
const selectIcon4       = [Db("\x04"), Db("\x04"), Db("\x04"), Db("\x08▶\x1E "), Db("\x04"), Db("\x04")];
const selectIcon5       = [Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x08▶\x1E "), Db("\x04")];
const selectIcon6       = [Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x04"), Db("\x08▶\x1E ")];
const selectIconList    = [selectIcon1, selectIcon2, selectIcon3, selectIcon4, selectIcon5, selectIcon6];
const guiUpdate         = PVariable();

const switchIcon        = [Db("\x07ON"), Db("\x08OFF")];


const blankText   = [
    Db("      "),
    Db("            "),
    Db("                  "),
    Db("                        "),
    Db("                              "),
    Db("                                    ")
];

function msqcInit(){
    EUDRegisterObjectToNamespace("move", moveStateMSQC);
}
